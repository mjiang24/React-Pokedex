import React from 'react';
import { AppBar, toolBar } from '@material-ui/core';

const Pokedex = () => {
    return (
        <div>
            pokedex page
        </div>
    )
}

export default Pokedex;